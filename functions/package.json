{
  "name": "remtori-serverside",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "rollup -c",
    "watch": "cross-env NODE_ENV=development rollup -wc",
    "serve": "firebase serve --only functions",
    "deploy": "firebase deploy --only functions",
    "start": "npm run serve"
  },
  "main": "dist/index.js",
  "engines": {
    "node": "12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase-admin": "^8.9.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^2.3.0",
    "@rollup/plugin-typescript": "^2.0.1",
    "@types/cors": "^2.8.6",
    "@types/node": "^13.1.2",
    "cross-env": "^7.0.0",
    "dotenv": "^8.2.0",
    "rollup": "^1.29.1",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
